import java.util.*;

public class Simulator {
    public static final int numLoop = 1000;
    public static final int numQuestions = 300;

    ArrayList<Person> AList = new ArrayList<Person>();
    ArrayList<Person> BList = new ArrayList<Person>();
    ArrayList<Person> CList = new ArrayList<Person>();
    HashMap<Person, ArrayList<Double>> CRatingDistri = new HashMap<Person, ArrayList<Double>>();

    public static void main(String[] args) {
        // initialise the lists of Person

        // get distribution for each C

            // for i in numLoop:


                // for j in numQuestions:
                    // get random A and B
                    // get A, B, C results
                    // get A, B, C correctness
                    // update rating of all three
                    // add rating of C into the C rating hashmap

        // get statistics for C of different accuracies

    }

}
